const handles = {
    namespace: '/',
    prefix: '-',
    reducers: {
        'LOGIN': (state, action) => {
            return state;
        },
        'LOGOUT': (state, action) => {
            return state;
        }
    },
    effects: {
        'LOGIN': action$ => [

        ],
    },
};

export default handles;
